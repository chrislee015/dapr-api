rootProject.name = "spring-monorepo"

include(
    "common",
    "config-server",
    "services:gateway",
    "services:api",
    "services:database",
    "services:batch",
    "services:messaging"
)
